fastapi==0.137.0
uvicorn[standard]==0.49.0
sqlalchemy==2.0.35
alembic==1.13.3
# Python 3.12 recommended (see backend/.python-version and Dockerfile).
psycopg[binary]==3.2.3
pydantic==2.13.4
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
python-multipart==0.0.12
email-validator==2.2.0
pypdf==5.1.0
python-docx==1.1.2
reportlab==4.2.5
google-generativeai==0.8.3
google-cloud-secret-manager==2.21.1
google-cloud-storage==3.12.1
google-cloud-aiplatform==1.161.0
# Pulled by agentplatform RAG (_gcs_utils); not declared as a hard dep of aiplatform.
pandas==2.2.3

# AI agent layer — Google Agent Development Kit (UC-01 review + prompt-edit).
# ADK can target Gemini (dev/pilot) or any on-prem/self-hosted model via LiteLLM,
# keeping legal-data inference inside JFPSL infra (see app/adk/models.py).
google-adk==2.2.0
litellm==1.89.0
# Required by SQLAlchemy's async engine (ADK DatabaseSessionService streams
# chat sessions over an async psycopg connection). Not pulled in transitively.
greenlet==3.1.1
# Free, keyless web search backend for the assistant's web_search tool
# (DuckDuckGo). Optional Google Programmable Search is used instead when
# google_cse_api_key + google_cse_id are configured.
ddgs==9.14.4
# Regulatory Intelligence scraper (UC-06): fetch + parse regulator pages/feeds.
httpx==0.28.1
beautifulsoup4==4.15.0
feedparser==6.0.12
