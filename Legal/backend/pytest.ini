[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    eval: retrieval-quality eval run; needs a live DB + embeddings. Skipped unless RUN_RAG_EVAL=1.
