apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "genaiforge.fullname" . }}-test-connection"
  labels:
    {{- include "genaiforge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "genaiforge.fullname" . }}-backend:{{ .Values.backend.service.port }}/health']
